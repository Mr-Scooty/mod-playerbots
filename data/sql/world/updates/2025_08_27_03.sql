-- Stage 2 (4.3.4 port): this file inserted custom bot marker spell 30758 ("aedm")
-- into the world `spell_dbc` override table using the 3.3.5a 234-column layout.
-- ShatterCore's 4.3.4 `spell_dbc` has a different column set (no `Category`, etc.),
-- so the original INSERT fails ("Unknown column 'Category'") and aborts the world DB update.
-- Neutered for now so the server boots; re-author this spell row in the 4.3.4 spell_dbc
-- column layout (or provide the marker spell via DBC/hotfix) when porting bot AoE markers.
